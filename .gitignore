# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Segredos locais — nunca versionados. .env.example continua rastreado como
# modelo do que precisa ser preenchido.
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rascunhos do programa de linha de comando do Supabase. São arquivos de
# cache da máquina de quem roda, não do projeto — nunca devem ir para o
# repositório.
supabase/.temp/
